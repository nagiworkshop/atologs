{
  "name": "atologs",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev:worker": "pnpm --filter @ccclub/worker dev",
    "deploy": "pnpm --filter @ccclub/worker run deploy"
  },
  "engines": {
    "node": ">=20"
  }
}
